# PostgreSQL
POSTGRES_USER=point
# 開発用の値をそのまま使わず、十分な長さのランダムな文字列を設定する
POSTGRES_PASSWORD=
POSTGRES_DB=point_system

# 公開するドメイン。Caddy がこの名前で証明書を自動取得する
# 例: point.example.com
SITE_ADDRESS=

# APIドキュメント（/api/docs, /api/openapi.json）を公開するか
ENABLE_API_DOCS=false
